# Extra dependencies for the Adaptive Quiz component.
# Install alongside the base requirements:
#   pip install -r requirements.txt -r requirements-adaptive-quiz.txt

# MongoDB + auth
motor>=3.6.0
beanie>=1.27.0
PyJWT>=2.10.0
bcrypt>=4.0.1,<5

# Learning-state prediction (RandomForest, in-process)
scikit-learn>=1.3.0
pandas>=2.0.0
joblib>=1.3.0

# Webcam expression model (Ultralytics YOLO — backend/model/best.pt)
opencv-python-headless>=4.8.0
ultralytics>=8.3.0
