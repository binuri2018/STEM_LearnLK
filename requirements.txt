# RAG tutor backend — Sri Lanka Grade 10–11 Science
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.6.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.0
python-multipart>=0.0.9

# PDF text extraction
pymupdf>=1.24.0

# Embeddings + vector search
sentence-transformers>=2.6.0
faiss-cpu>=1.8.0
numpy>=1.26.0
certifi>=2024.0.0
rank-bm25>=0.2.2
Pillow>=10.0.0

# LLM (optional: set OPENAI_API_KEY; otherwise configure Ollama)
openai>=1.30.0
httpx>=0.27.0

# Knowledge Maps (Member 4) — YouTube transcript fetch + suggestions
youtube-transcript-api>=0.6.2
google-api-python-client>=2.100.0

# Knowledge Maps (Member 4) — web search fallback for claim verification
duckduckgo-search>=6.0.0

# Narrative Learning (persona + Chroma RAG + Groq/Ollama writer)
pandas
openpyxl
scikit-learn
langchain-chroma
langchain-ollama
langchain-groq
langchain-openai
langchain-core
chromadb>=1.3.5,<2
